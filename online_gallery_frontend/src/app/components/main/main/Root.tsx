'use client'

import {NavigationComponent} from "@/app/components/main/main/navigation/NavigationComponent";

export const Root = () => {

    return (
        <section>
            <nav>
                <NavigationComponent/>
            </nav>
            <main>

            </main>
        </section>
    )
}